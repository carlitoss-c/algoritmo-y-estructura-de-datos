Algoritmo MejorPromedio
	Definir i, nota1, nota2, nota3 Como Real
	Definir promedio, maxPromedio Como Real
	Definir nombre, mejorAlumno Como Cadena
	
	maxPromedio <- -1
	
	Para i <- 1 Hasta 10 Hacer
		Escribir "Ingrese el nombre del alumno ", i, ":"
		Leer nombre
		Escribir "Ingrese la primera nota:"
		Leer nota1
		Escribir "Ingrese la segunda nota:"
		Leer nota2
		Escribir "Ingrese la tercera nota:"
		Leer nota3
		
		promedio <- (nota1 + nota2 + nota3) / 3
		
		Si promedio > maxPromedio Entonces
			maxPromedio <- promedio
			mejorAlumno <- nombre
		FinSi
	FinPara
	
	Escribir "El mejor promedio es ", maxPromedio, " y corresponde a ", mejorAlumno
FinAlgoritmo
