Algoritmo SumaYPromedioPositivos
	Definir n, suma, contador Como Entero
	Definir promedio Como Real
	
	suma <- 0
	contador <- 0
	
	Escribir "Ingrese un número (negativo para terminar):"
	Leer n
	
	Mientras n >= 0 Hacer
		suma <- suma + n
		contador <- contador + 1
		
		Escribir "Ingrese otro número (negativo para terminar):"
		Leer n
	FinMientras
	
	Si contador > 0 Entonces
		promedio <- suma / contador
		Escribir "La suma de positivos es: ", suma
		Escribir "El promedio de positivos es: ", promedio
	Sino
		Escribir "No se ingresaron números positivos."
	FinSi
	
FinAlgoritmo
